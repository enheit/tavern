import { cn } from "@/lib/utils";

// Placeholder route shell (frozen test id `page-register`). Real register UI lands in a later step.
export function RegisterPage() {
  return (
    <div
      data-testid="page-register"
      className={cn("flex h-full w-full items-center justify-center")}
    />
  );
}
