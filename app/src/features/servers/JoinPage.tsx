import { cn } from "@/lib/utils";

// Placeholder route shell (frozen test id `page-join`). Real join/create UI lands in a later step.
export function JoinPage() {
  return (
    <div data-testid="page-join" className={cn("flex h-full w-full items-center justify-center")} />
  );
}
