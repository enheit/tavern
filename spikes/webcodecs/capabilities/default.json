{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capability. Custom app commands (probe_frames/save_results/exit_app) are local-origin, non-plugin, and need no ACL entry (tauri webview/mod.rs gate).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
