// Single source for every magic number in the product (PLAN App-B).
export const LIMITS = {
  usernameRe: /^[a-z0-9_]{3,20}$/,
  displayNameMin: 1,
  displayNameMax: 32,
  passwordMinLen: 8,
  serverNicknameRe: /^[a-z0-9-]{3,32}$/i,
  serverPasswordMinLen: 4,
  colorRe: /^#[0-9a-f]{6}$/,
  avatarMaxBytes: 2_000_000,
  avatarSizePx: 256,
  messageMaxChars: 2000,
  statusMaxChars: 128,
  historyPageSize: 50,
  soundMaxBytes: 10_000_000,
  soundMaxDurationMs: 300_000,
  soundMinTrimMs: 200,
  soundNameMax: 32,
  recordingMaxDurationMs: 14_400_000,
  recordingTimesliceMs: 10_000,
  recordingPartBytes: 5_242_880,
  screenshotMaxBytes: 15_000_000,
  rateScreenshotsPerMin: 30,
  // Pasted chat image (§ chat image paste): re-encoded to webp client-side and capped to
  // `chatImageMaxDimPx` on the longest edge before upload, so the stored object stays modest.
  chatImageMaxBytes: 10_000_000,
  chatImageMaxDimPx: 2560,
  maxConcurrentScreenShares: 4,
  maxServersPerUser: 20,
  maxMembersPerServer: 25,
  speakingRmsThreshold: 0.02,
  speakingSustainMs: 100,
  speakingHangoverMs: 300,
  wsTicketTtlMs: 30_000,
  helloTimeoutMs: 5_000,
  pingIntervalMs: 30_000,
  reconnectCapMs: 30_000,
  emptyVoiceCloseMs: 60_000,
  rateAuthPerIpPerMin: 10,
  rateChatPerSec: 5,
  rateChatBurst: 10,
  rateSoundPlayPerSec: 1,
  rateUploadsPerHour: 10,
  rateRtcOpsPerMin: 60,
  egressWarnGB: 700,
  egressKillGB: 900,
} as const;
