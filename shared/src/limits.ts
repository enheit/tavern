// Single source for every magic number in the product (PLAN App-B).
export const LIMITS = {
  usernameRe: /^[a-z0-9_]{3,20}$/,
  displayNameMin: 1,
  displayNameMax: 32,
  passwordMinLen: 8,
  serverNicknameRe: /^[a-z0-9-]{3,32}$/i,
  serverPasswordMinLen: 4,
  colorRe: /^#[0-9a-f]{6}$/,
  avatarMaxBytes: 2_000_000,
  avatarSizePx: 256,
  messageMaxChars: 2000,
  reactionEmojiMaxChars: 32,
  statusMaxChars: 128,
  historyPageSize: 30,
  soundMaxBytes: 10_000_000,
  soundMaxDurationMs: 300_000,
  soundMinTrimMs: 200,
  soundNameMax: 32,
  soundFileNameMax: 255,
  soundGainMin: 0,
  soundGainMax: 2,
  soundMultipartOverheadBytes: 100_000,
  recordingMaxDurationMs: 14_400_000,
  recordingTimesliceMs: 10_000,
  recordingPartBytes: 5_242_880,
  screenshotMaxBytes: 15_000_000,
  rateScreenshotsPerMin: 30,
  // Stream teasers are deliberately small, lossy stills. A publisher refreshes its one stable R2
  // object about once a minute; the Worker persists the shorter interval as an abuse/race guard.
  streamPreviewMaxBytes: 500_000,
  streamPreviewMaxWidthPx: 640,
  streamPreviewMaxHeightPx: 360,
  streamPreviewRefreshMs: 60_000,
  streamPreviewMinUploadIntervalMs: 55_000,
  // Pasted chat image (§ chat image paste): re-encoded to webp client-side and capped to
  // `chatImageMaxDimPx` on the longest edge before upload, so the stored object stays modest.
  chatImageMaxBytes: 10_000_000,
  chatImageMaxDimPx: 2560,
  maxConcurrentScreenShares: 4,
  maxServersPerUser: 20,
  maxMembersPerServer: 25,
  speakingRmsThreshold: 0.02,
  speakingSustainMs: 100,
  speakingHangoverMs: 300,
  wsTicketTtlMs: 30_000,
  helloTimeoutMs: 5_000,
  pingIntervalMs: 30_000,
  reconnectCapMs: 30_000,
  emptyVoiceCloseMs: 60_000,
  hangoutMinOverlapMs: 60_000,
  hangoutReconnectGraceMs: 60_000,
  homeHangoutLimit: 5,
  rateAuthPerIpPerMin: 10,
  rateChatPerSec: 5,
  rateChatBurst: 10,
  rateReactionPerSec: 5,
  rateReactionBurst: 10,
  rateSoundPlayPerSec: 1,
  rateUploadsPerHour: 10,
  rateRtcOpsPerMin: 60,
  egressWarnGB: 700,
  egressKillGB: 900,
  pointRateMaxPerMinute: 1000,
  pointDailyCapMax: 1_000_000,
  marketItemNameMax: 32,
  marketPriceMax: 1_000_000,
  marketIconInputMaxBytes: 20_000_000,
  marketIconMultipartOverheadBytes: 100_000,
  marketIconOutputMaxBytes: 2_000_000,
  marketIconSizePx: 48,
  pollQuestionMaxChars: 45,
  pollOutcomeMaxChars: 25,
  pollOutcomeMin: 2,
  pollOutcomeMax: 10,
  pollDurationMinSeconds: 30,
  pollDurationMaxSeconds: 1800,
  pollMaxUnresolvedPerCreator: 3,
  pollCorrectionHoldMs: 5 * 60_000,
  pollResultVisibleMs: 8_000,
  pollResolveTimeoutMs: 24 * 60 * 60_000,
} as const;
