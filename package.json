{
  "name": "tavern",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev:worker": "pnpm --filter tavern-worker dev",
    "dev:app": "pnpm --filter tavern-app dev",
    "tauri": "tauri",
    "gen:protocol": "TS_RS_EXPORT_DIR=../../worker/src/protocol cargo test -p tavern-protocol --quiet && TS_RS_EXPORT_DIR=../../app/src/lib/protocol cargo test -p tavern-protocol --quiet"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
